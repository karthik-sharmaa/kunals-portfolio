import Typed from 'typed.js'

var options = {
  strings: ["I'm a Software Engineer", 'I build low latency websites', "I Solve Problems"],
  typeSpeed: 30,
  backSpeed: 10,
  backDelay: 1500,
  loop: true,
}

var typed = new Typed('#headline', options)
